
export interface Listed {
        price: number;// Change to BigInt if needed
        from: string;
        to: string;
        tokenId: number;
    timestamp: number;
    status: number;
    }

export const Status = [""]
